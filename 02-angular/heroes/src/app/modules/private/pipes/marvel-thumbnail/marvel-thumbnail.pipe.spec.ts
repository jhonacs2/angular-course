import { MarvelThumbnailPipe } from './marvel-thumbnail.pipe';

describe('MarvelThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new MarvelThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
