import { TmChartDirective } from './tm-chart.directive';

describe('TmChartDirective', () => {
  it('should create an instance', () => {
    const directive = new TmChartDirective();
    expect(directive).toBeTruthy();
  });
});
